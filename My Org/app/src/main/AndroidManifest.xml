<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gwazasoftwares.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnnouncementsViewActivity"
            android:label="Announcements"></activity>
        <activity
            android:name=".BranchesViewActivity"
            android:label="Org Branches" />
        <activity
            android:name=".FeedBackViewActivity"
            android:label="Employee Feedback" />
        <activity
            android:name=".EmployeeDetails"
            android:label="Details" />
        <activity
            android:name=".EmployeesView"
            android:label="Employee Details" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".Admin"
            android:label="@string/title_activity_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login" />
    </application>

</manifest>